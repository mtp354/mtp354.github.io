name: Quantum Radar — Jobs

on:
  workflow_dispatch:
  schedule:
    - cron: "19 14 * * *"

permissions:
  contents: write

jobs:
  jobs:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: projects/quantum-radar
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: projects/quantum-radar/requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Aggregate LinkedIn quantum jobs
        env:
          LINKEDIN_JOBS_FEED_URL: ${{ secrets.LINKEDIN_JOBS_FEED_URL }}
        run: python scripts/fetch_jobs.py

      - name: Commit updates
        working-directory: ${{ github.workspace }}
        run: |
          if [ -z "$(git status --porcelain -- projects/quantum-radar/state/jobs.json _quantum_radar)" ]; then
            echo "No job changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add projects/quantum-radar/state/jobs.json _quantum_radar
          git commit -m "chore(quantum-radar): refresh jobs"
          git push origin HEAD:${{ github.ref_name }}
