name: Quantum Radar — Conferences

on:
  workflow_dispatch:
  schedule:
    - cron: "47 12 */3 * *"

permissions:
  contents: write

jobs:
  conferences:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: projects/quantum-radar
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: projects/quantum-radar/requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Aggregate conferences
        run: python scripts/fetch_conferences.py

      - name: Commit updates
        working-directory: ${{ github.workspace }}
        run: |
          if [ -z "$(git status --porcelain -- projects/quantum-radar/state/conferences.json _quantum_radar)" ]; then
            echo "No conference changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add projects/quantum-radar/state/conferences.json _quantum_radar
          git commit -m "chore(quantum-radar): refresh conferences"
          git push origin HEAD:${{ github.ref_name }}
